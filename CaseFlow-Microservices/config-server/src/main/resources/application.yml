server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/soham-dutta-cog/caseflow_config_server.git
          default-label: rashad-config
          clone-on-start: true
          skip-ssl-validation: true
          username: rashadahmedz
          password: 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

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true